spring:
  # DataSource Config
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/facetoface?useUnicode=true&characterEncoding=UTF-8&useSSL=false
    username: root
    password: 123456
  # session 失效时间
  session:
    timeout: 86400
server:
  address: 0.0.0.0